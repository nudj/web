FROM node:6.11.1-alpine
ARG NPM_TOKEN
RUN mkdir -p /usr/src && apk update && apk add zsh make
WORKDIR /usr/src
COPY src /usr/src
RUN npm i
EXPOSE 80 81 82
CMD ./node_modules/.bin/standard && ./node_modules/.bin/mocha --recursive test
