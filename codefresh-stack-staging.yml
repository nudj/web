version: '1.0'

steps:

  create_stack:
    image: nudj/devops:aws
    description: Create or update the service stack
    commands:
      - /usr/src/scripts/create-service.sh -s core-vpc -f ./service.json -e staging
